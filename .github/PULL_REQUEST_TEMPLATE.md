## Summary

<!-- What changed and why? -->

## Checklist

- [ ] The affected module's tests pass (`GOWORK=off go test ./...` in the module, or `npm run check` + tests for web)
- [ ] Bug fixes include a regression test that fails on the buggy version
- [ ] No hand-edits to generated code
- [ ] Documentation updated where behavior changed

Fixes #
